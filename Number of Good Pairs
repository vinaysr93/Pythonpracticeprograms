class Solution:

    def numIdenticalPairs(self, nums):
        count=0
        for x in range(len(nums)-1):
            for y in range(x+1,len(nums)):
                if nums[x]==nums[y]:
                    count=count+1
    
        return count


        
