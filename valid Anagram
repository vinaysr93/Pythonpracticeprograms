class Solution:
    def isAnagram(self, s: str, t: str):
        
        if len(s)!=len(t):
            return False
        
        k=sorted(s)
        l=sorted(t)
        
        if k==l:
            return True
        else:
            return False
        
