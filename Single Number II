class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        
        
      

        for x in nums:
           
                s=nums.count(x)
                if s!=3:
                    return x
                
