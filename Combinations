class Solution:
    def combine(self, n: int, k: int) -> List[List[int]]:
        
        
        ar=[]
        for x in range(1,n+1):
            ar.append(x)


        return(list(combinations(ar,k)))
